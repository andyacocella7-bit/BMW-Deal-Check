# BMW DealCheck MVP

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy
Put `app.py` and `requirements.txt` into a GitHub repository, then deploy the repository through Streamlit Community Cloud.

## Next version
Replace the heuristic estimate with licensed/current used-car market data and a proper model trained on historical comparable listings.
